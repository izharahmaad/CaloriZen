import { StyleSheet } from "react-native";
import { Colors } from "./colors";

export const T = StyleSheet.create({
  screen: { flex: 1, backgroundColor: Colors.bg },
  h1: { color: Colors.text, fontSize: 28, fontWeight: "900", marginTop: 12 },
});
